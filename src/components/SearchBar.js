import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SearchBar = () => {
  return <Text>Home Bar</Text>;
};

const styles = StyleSheet.create({});

export default SearchBar;
